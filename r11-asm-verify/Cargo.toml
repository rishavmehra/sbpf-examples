[package]
name = "r11-asm-verify"
version = "0.1.0"
edition = "2021"
description = "Verifies the r11->r10 offset math on Claire's hand-written assembly, independent of LLVM and the linker"

[dependencies]
sbpf-assembler = "0.2.4"
mollusk-svm = "0.14.0"
solana-instruction = "3.4.0"

[workspace]
